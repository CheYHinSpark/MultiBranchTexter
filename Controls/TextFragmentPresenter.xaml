<UserControl x:Class="MultiBranchTexter.Controls.TextFragmentPresenter"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>
        <StackPanel x:Name="opContainer" Orientation="Vertical" Grid.Row="0"/>
        <TextBox x:Name="contentContainer" Grid.Row="1" 
                 AcceptsReturn="True" AcceptsTab="True" TextWrapping="Wrap"
                 LostFocus="fragment_LostFocus"/>

        <Button x:Name="addOpBtn" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Top"
                Width="20" Height="8" Click="addOpBtn_Click">
            +
        </Button>
        <Button x:Name="addFragmentBtn" Grid.Row="2" Width="40" HorizontalAlignment="Left"
                Click="addFragmentBtn_Click"/>
        <Button x:Name="closeFragmentBtn" Grid.Row="2" Width="40" HorizontalAlignment="Right"
                Click="closeFragmentBtn_Click"/>
    </Grid>
</UserControl>
